apiVersion: v1
kind: Pod
metadata:
  name: env-configmap
spec:
  containers:
  - name: nginx
    command:  ["/bin/sh", "-c", "printenv"]
    image: nginx
    envFrom:
    - configMapRef:
        name: myconfigmap